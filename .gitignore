# third-party tools: python setup.py downloads them from their official GitHub releases
external/
# game-derived data, produced from YOUR installed game by extract_menu_music.py / rebuild.py: never commit, never redistribute
versions/
render/wem/
render/banks/
render/txtp/
audio/renders/
release/*.zip
**/*.WBNK
**/packagedefinition.txt.original*
__pycache__/
# committed on purpose: mods/*/chunk0patchN.rpkg (a rebuilt 19 KB soundbank without any media) and the
# patchlevel=310 manifest - the same two files every community RPKG mod ships.
